services:
  - type: web
    name: caldir-relay
    runtime: node
    plan: free
    region: frankfurt
    branch: master
    rootDir: relay
    buildCommand: npm install && npm run build
    startCommand: node dist/main.js
    healthCheckPath: /
    autoDeploy: true
