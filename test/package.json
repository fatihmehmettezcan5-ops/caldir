{
  "name": "@caldir/test",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "dependencies": {
    "@caldir/shared": "0.1.0",
    "@caldir/server": "0.1.0",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "20",
    "typescript": "5.5"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "smoke": "node dist/smoke.js"
  }
}